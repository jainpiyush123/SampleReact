import { connect } from 'react-redux'
import ProductGrid from './component'
const ProductGridContainer = connect(
  (state) => ({
  }),
  {

  }
)(ProductGrid)
export default ProductGridContainer
