import { connect } from 'react-redux'
import ProductRegular from './component'
const ProductRegularContainer = connect(
  (state) => ({
  }),
  {

  }
)(ProductRegular)
export default ProductRegularContainer
