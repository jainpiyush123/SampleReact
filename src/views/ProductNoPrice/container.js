import { connect } from 'react-redux'
import ProductNoPrice from './component'
const ProductNoPriceContainer = connect(
  (state) => ({
  }),
  {
  
  }
)(ProductNoPrice)
export default ProductNoPriceContainer
